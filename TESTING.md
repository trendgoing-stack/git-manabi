# 実機確認チェックリスト（TESTING.md）

iPhone（iOS 17 以降）の Safari と、ホーム画面に追加したアプリの両方で確認します。クリップボードや Service Worker は HTTPS でしか動かないため、GitHub Pages に push してから確認してください。

## 検索

- [ ] 検索のトップ画面の上部に「Gitまなび帳」と表示される
- [ ] 日本語で検索できる（例：「コミットを取り消し」「ブランチ 削除」）
- [ ] カタカナで検索できる（例：「ステージ」「スタッシュ」）。半角カナ（ｽﾀｯｼｭ）でも見つかる
- [ ] ひらがなの読みで検索できる（例：「とりけし」「りべーす」）
- [ ] 英字で検索できる（例：「rebase」「REBASE」「ｒｅｂａｓｅ」（全角））
- [ ] コマンド名で前方一致する（例：「git sw」で switch 系が上位に出る）
- [ ] 空白区切りで AND 検索になる（例：「push 取り消し」）
- [ ] ツール（git／gh／Web／自作）とカテゴリで絞り込める
- [ ] 0 件のときに「困った」への導線が出る
- [ ] 検索語が空のとき、カテゴリ一覧とシーン別チートシートが出る
- [ ] 自作スニペットが検索結果に「自作」バッジ付きで出る

## コマンドビルダーとコピー

- [ ] プレースホルダに入力すると完成形が更新される。未入力の部分は `<ラベル>` のまま表示される
- [ ] 「コピー」でコピーでき、「コピーしました」と表示される
- [ ] 未入力が残っていてもコピーでき、「未入力の項目があります」も表示される
- [ ] コピーした完成形をターミナルに貼り付けて、意図どおり動く（git-commit の `"` を含むメッセージ、git-config-user の2行のコマンドなど）
- [ ] 値に `$` などを入れると、シェルが解釈する文字の注意が出る
- [ ] オプションのチップで ON／OFF でき、完成形のオプションの順番が定義順になる（git-log など）
- [ ] 同時に使えないオプションがグレーアウトする（gh-pr-merge の `--merge`／`--squash`／`--rebase`、git-pull の `--rebase`／`--ff-only`）
- [ ] 値が必要なオプションを ON にすると入力欄が出る（git-log の `-n`）
- [ ] 使用例の「コピー」ボタンでもコピーできる
- [ ] 画面を離れて戻ると、入力値が消えている

## 危険コマンド

- [ ] 「危険」の項目（git-reset-hard、git-restore、git-clean、git-push-force-lease など）をコピーするとき、危険な理由と元に戻す方法の確認ダイアログが出る
- [ ] ダイアログの「コピーする」でコピーできる
- [ ] 設定で「危険なコマンドのコピー時に確認する」をオフにすると、ダイアログが出ずにコピーされる

## 入力と表示

- [ ] 検索欄、プレースホルダ、メモ、スニペットの入力欄をタップしても画面が拡大されない
- [ ] 上部のバーと下部のタブバーが、ノッチ・ダイナミックアイランド・ホームバーに重ならない
- [ ] ボタンやタブが押しやすい（小さすぎない）
- [ ] 設定の文字サイズ「大」で全体が大きくなる
- [ ] ダークモードに切り替えると配色が変わり、文字が読める

## ブランチ図

- [ ] ライトモードで、図（git-merge、git-rebase、git-reset-hard など）が読める
- [ ] ダークモードで、図が読める
- [ ] 横に長い図は、図の部分だけ横にスクロールでき、ページ全体は横に動かない

## 困った（フローチャート）

- [ ] 選択肢を選ぶと次の質問に進み、パンくずに回答が並ぶ
- [ ] 「1つ戻る」で1つ前の質問に戻る
- [ ] ブラウザの戻る（画面左端からのスワイプ）でも1つ前の質問に戻る
- [ ] 「最初からやり直す」で最初の質問に戻る
- [ ] 結果の項目をタップすると詳細画面が開き、戻ると結果に戻る

## 学ぶ

- [ ] ツールやカテゴリで出題範囲を絞れる。出題できる項目が4件未満の範囲では、開始できないことが表示される
- [ ] 「GitHub画面」だけを選んでも出題でき、説明文からやりたいことを選ぶ問題が出る
- [ ] 10問で結果が出て、間違えた問題が一覧になる
- [ ] 「苦手を復習」で、間違えた問題が不正解率の高い順に出る
- [ ] フラッシュカードを左右にスワイプすると次・前のカードに移る
- [ ] カードの上で縦にスワイプすると、ページが普通にスクロールする
- [ ] カードをタップすると裏返る
- [ ] 用語集が五十音順に並び、検索できる。本文の用語（点線付き）をタップすると定義が出る

## マイ

- [ ] お気に入りの追加・解除・並べ替えができる
- [ ] 閲覧履歴が新しい順に出て、全消去できる
- [ ] メモが保存され、画面を開き直しても残っている。「メモ」の一覧に出る
- [ ] スニペットの追加・編集・複製・削除ができ、`<name>` が入力欄になる

## データ管理

- [ ] Safari でエクスポートした（ファイル保存またはテキストのコピー）データを、ホーム画面のアプリでインポートできる
- [ ] 「統合」で今のデータが残り、「置き換え」で置き換わる
- [ ] 形式の違うファイルやテキストを読み込むと、エラーが出て何も変わらない
- [ ] 「すべてのデータを削除」で確認ダイアログが出て、削除される

## PWA・オフライン

- [ ] ホーム画面に「まなび帳」という名前と、アイコンが表示される
- [ ] ホーム画面から起動すると、Safari の枠なしで開く
- [ ] 一度開いた後、機内モードにしてホーム画面から起動し、全タブ（検索・困った・学ぶ・マイ・設定）が動く
- [ ] 機内モードでも詳細画面、ブランチ図、クイズ、フラッシュカード、用語集が表示される
- [ ] `sw.js` の `VERSION` を上げて push した後、アプリを開き直すと「更新があります（タップで再読み込み）」が出て、タップすると新しい内容になる

## アクセス解析（GoatCounter）

- [ ] 送信オン（既定）のとき、GoatCounter の管理画面で、アプリを開いた回数だけ `/git-manabi/` が数えられる（画面遷移や検索では増えない）
- [ ] 設定で「アクセス解析を送信しない」をオンにして開き直すと、数が増えない（PC の開発者ツールの「ネットワーク」で count.js が読み込まれないことも確認できる）
- [ ] 機内モードで開いても、エラーが表示されない

## ヘルプ

- [ ] 設定の「ヘルプ・注意事項」から、注意書き（オリジナルの要約であること、危険なコマンドの前のバックアップ、GitHub の画面が変わること、Safari とホーム画面で保存データが別なこと、アクセス解析で送る内容）が読める
